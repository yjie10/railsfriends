**Rails is what is called the MVC architecture -- `Model`, `View`, `Controller`.**

- `Models` are databases.
- `Views` are webpages.
- `Controllers` are sort of like the brains behind the scene. Logic.
